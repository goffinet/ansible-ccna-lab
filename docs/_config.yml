# Site settings
title: Ansible CCNA Lab
email: your-email@domain.com
description: >
  CCNA Labs ported on Ansible -
  for education purpose only -
  IOSv and IOSvL2 support only
lang: fr-FR

baseurl: "/ansible-ccna-lab" # the subpath of your site, e.g. /blog/
url: https://goffinet.github.io/ansible-ccna-lab # the base hostname & protocol for your site
git_address: https://github.com/goffinet/ansible-ccna-lab
git_edit_address: https://github.com/goffinet/ansible-ccna-lab/blob/master/docs

# theme options from https://bootswatch.com/3/
# comment out this to use default Bootstrap
bootwatch: cyborg # paper cerulean cosmo custom cyborg darkly flatly journal lumen readable sandstone simplex slate solar spacelab superhero united yeti

# Build settings
markdown: kramdown
highlighter: rouge
# Plugins (previously gems:)
plugins:
  - jekyll-feed
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap

exclude:
  - Gemfile
  - Gemfile.lock
  - .idea/
  - .gitignore
  - README.md
timezone: Europe/Paris
defaults:
- scope:
    path: _posts
    type: posts
  values:
    layout: post
    sectionid: blog

- scope:
    path: _docs
    type: docs
  values:
    layout: docs
    sectionid: docs
    seo:
      type: "WebPage"

collections:
  docs:
    permalink: /:collection/:path/
    output: true
  posts:
    permalink: /blog/:year/:month/:day/:title/
    output: true
