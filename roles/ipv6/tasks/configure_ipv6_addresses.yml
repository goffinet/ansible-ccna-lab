---
- name: enable ipv6 and configure link-local address
  ios_config:
    defaults: yes
    parents: "interface {{ item.0.id }}"
    lines:
      - "ipv6 enable"
      - "ipv6 address {{ item.1 | upper }} link-local"
  loop: "{{ interfaces|subelements('ipv6_link_local_addresses', skip_missing=True) }}"
  when:
    - item.1 | ipaddr('FE80::/16')
    - item.1 | ipv6
- name: configure ula/uga ipv6 addresses
#  ios_l3_interfaces:
#    config:
##      - name: "{{ item.0.id }}"
##        ipv6:
##        - address: "{{ item.1 }}"
  ios_config:
    defaults: yes
    parents: "interface {{ item.0.id }}"
    lines:
      - "ipv6 address {{ item.1 | upper }}"
  loop: "{{ interfaces|subelements('ipv6_addresses', skip_missing=True) }}"
  when:
    - item.1 | ipaddr('FD00::/8') or item.1 | ipaddr('2000::/3')
    - item.1 | ipv6
