---
- name: configure ipv4 addresses
  ios_l3_interfaces:
    config:
      - name: "{{ item.id }}"
        ipv4:
        - address: "{{ item.ipv4_addresses }}"
  loop: "{{ interfaces }}"
  loop: interfaces.ipv4_addresses is defined
