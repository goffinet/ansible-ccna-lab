---
- name: redistribute static nat route to rip
  ios_config:
    defaults: yes
    parents: router rip
    lines:
      - default-information originate
  when:
    - routing.rip is defined
    - nat is defined
